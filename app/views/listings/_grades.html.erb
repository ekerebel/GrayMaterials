<% if !@grades.blank? %>
    <%= collection_select :listing, :grade_id, @grades, :id, :name, {prompt: "Choose a Grade"}, {class: "form-control", id: "grade_select"} %>
<% else %>
    <%= collection_select :listing, :grade_id, Grade.all, :id, :name, {prompt: "Choose a Grade"}, {class: "form-control", id: "grade_select"} %>
<% end %>